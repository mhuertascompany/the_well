# @package _global_
# Noctua HPC cluster configuration
# Adjust well_base_path to wherever the datasets are stored on the cluster.
data:
  well_base_path: "__PLACEHOLDER__/datasets/"   # absolute path on Noctua
experiment_dir: "__PLACEHOLDER__/experiments/"  # absolute path for outputs
